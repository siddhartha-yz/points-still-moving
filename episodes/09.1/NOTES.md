# 9.1 片外说明

这一页给看完默片的人。**不进视频。**

教材：[D2L 9.1 门控循环单元（GRU）](https://zh.d2l.ai/chapter_recurrent-modern/gru.html)

## 先说问题

循环网络每走一步，都要决定上一刻的隐状态还保留多少。完全覆盖会很快忘掉，完全照搬又不会吸收新输入。GRU 的更新门把这件事拆成每个分量各自的比例。

默片只演这个比例。光晕框住的是旧状态 \(H_{t-1}\)：它是系列旅行者在循环章节里的形态，不是前面散点图里的同一个数。

## 画面里在干什么

1. **半秒 `9.1`**：章节号。
2. **左侧蓝色 \(H_{t-1}\)**：旧状态是 \([0.60,-0.40,0.20]^\top\)。
3. **黄色 \(\tilde H_t\)**：候选状态是 \([-0.20,0.80,-0.60]^\top\)。三条横轨道一行对应一个隐藏单元。
4. **门值 \(Z_t\)**：\([0.82,0.25,0.60]^\top\)。白点从候选端落在轨道上的位置，显示它距候选端的比例正是 \(z_i\)：第一行靠旧状态，第二行靠候选，第三行居中偏旧。
5. **右侧 \(H_t\)**：逐分量计算
   \[
   H_t=Z_t\odot H_{t-1}+(1-Z_t)\odot\tilde H_t
   =[0.456,0.500,-0.120]^\top.
   \]

## 刻意没演

重置门、候选状态如何由输入与重置后的历史算出、时间反向传播、训练。这里只锁定更新门「保留多少旧状态」这一件事。

## B 站可粘贴

**标题**

点还在动 · 9.1 GRU 更新门

**简介**

旧隐状态和候选状态并不是二选一。更新门在每个分量上选一个比例，把它们混成下一步的隐状态。没有旁白。

《动手学深度学习》9.1：https://zh.d2l.ai/chapter_recurrent-modern/gru.html
