# 9.4 片外说明

这一页给看完默片的人。**不进视频。**

教材：[D2L 9.4 双向循环神经网络](https://zh.d2l.ai/chapter_recurrent-modern/bi-rnn.html)

## 先说问题

普通循环网只从左往右看。填空、对人名划标签这种题，右边的词也在说话。书里的办法是再跑一个从右往左的隐状态，每个时间步把两个方向拼成一根更长的向量。

默片只演这一件：四个词元排成一条很短的序列，黄针从左到右长出来，蓝针从右到左长出来，然后在 \(t=3\) 把两根针收成带光晕的 \(H_3\)。没有旁白。权重是写死的，不是学来的。

## 画面里在干什么

1. **半秒 `9.4`**：章节号。
2. **四个词元先停住**。蓝黄只是点的身份。数是 \(x=(1.00,\,0.50,\,-0.80,\,0.60)\)。上面那条零轴标 `→`，下面那条标 `←`。
3. **公式出现一次**：\(H_t=[\overrightarrow h_t,\,\overleftarrow h_t]\)。先写公式，针还没长。
4. **左到右**：\(\overrightarrow h_t=\tanh(W_{xh}^{(f)}x_t+W_{hh}^{(f)}\overrightarrow h_{t-1}+b_h^{(f)})\)。四步，每步约两秒。这一条是 \(+0.782,\,+0.671,\,-0.579,\,+0.361\)。第三根朝下，因为这个 \(x\) 是负的。
5. **右到左**：\(\overleftarrow h_t=\tanh(W_{xh}^{(b)}x_t+W_{hh}^{(b)}\overleftarrow h_{t+1}+b_h^{(b)})\)。从最后一个词元往回走。这一条是 \(-0.570,\,+0.096,\,+0.629,\,-0.327\)。
6. **圈住 \(t=3\) 的拼接**。光晕在拼接向量上，不是在某个词元上。左侧固定写 \(\overrightarrow h_3=-0.579\)、\(\overleftarrow h_3=+0.629\)、\(H_3=[-0.579,+0.629]\)。两个方向符号相反：同一时刻，过去和未来说的不是同一句话。最后一帧两排针都还在。

## 刻意没演

输出层 \(O_t=H_t W_{hq}+b_q\)、训练、拿双向网当语言模型（书里 9.4.3 的反例：`time travellerererere`）。下一集若走 9.6，才轮到编码器把变长输入压成固定状态。

## B 站可粘贴

**标题**

点还在动 · 9.4 双向循环神经网络

**简介**

左到右一根针，右到左一根针，同一个 t 拼起来。没有旁白。

《动手学深度学习》9.4：https://zh.d2l.ai/chapter_recurrent-modern/bi-rnn.html

圈住的是拼接后的隐状态。源码与成片：https://github.com/siddhartha-yz/points-still-moving/releases/tag/09.4
